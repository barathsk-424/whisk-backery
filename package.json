{
  "name": "bakery-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:frontend": "npm run dev --prefix thewhisk",
    "dev:backend": "npm run dev --prefix backend",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "build": "npm run build --prefix thewhisk",
    "preview": "npm run preview --prefix thewhisk"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "vite": "^8.0.3"
  }
}
